# Recall: 2:45 demo recording script

## 0:00 - 0:18: the problem

"AI media teams do not just pay to generate. They pay again when the best output gets lost, a teammate needs it next week, or a small change turns into a blind reroll. Recall makes that unnecessary."

Show the workspace and the generation-spend / saved-by-Recall cards.

## 0:18 - 0:45: the reusable record

"Every Recall generation keeps the original B2 asset, the Genblaze recipe, its canonical provenance hash, cost, and lineage together. This is generation memory, not another gallery."

Open the cobalt showcase asset and click **Proof**.

## 0:45 - 1:05: prove it

"The stored B2 bytes re-hash to the recorded SHA-256. The Genblaze manifest is beside them. This is the exact asset, not a new image that happens to look similar."

Point to B2 asset hash and Genblaze manifest receipt.

## 1:05 - 1:28: save money

"When a team needs the original again, Retrieve returns the B2 object. There is no provider call and no new charge. Recall records the avoided replacement cost."

Click **Retrieve** and refresh the saved metric.

## 1:28 - 1:52: stop duplicates before they happen

"Before a paid request, Recall asks the library first. This is a semantic match, so it can catch a related need even when the wording changes. The creator chooses reuse or a deliberate new run."

Enter a related cobalt/product-tile prompt and show Reuse Gate. Choose Retrieve free.

## 1:52 - 2:17: controlled variation

"If the requirement really changed, we fork the known-good asset instead of gambling from zero. This emerald variation is a real Genblaze child run linked to its cobalt parent."

Show the emerald fork Proof screen and lineage chain.

## 2:17 - 2:35: production protection

"When a creative is approved, Recall copies it to an Object Lock-protected B2 final. The record is durable, inspectable, and safe from an accidental rewrite."

Show the approved/locked state.

## 2:35 - 2:45: closing

"Backblaze B2 stores the economic memory. Genblaze preserves the reproducible route. Recall makes the best default: generate once, reuse forever."

End on the savings metric and `Generate once. Reuse forever.`